{
  "name": "elixir-cod-app",
  "version": "0.1.0",
  "private": true,
  "description": "COD Fee service — auto-adds the Cash on Delivery fee to COD orders (Phase 1: single store).",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "register": "node register-webhook.mjs"
  }
}
